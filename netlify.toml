[build]
  command = "npm run build"
  publish = "dist"

# A single page app serves every route from index.html, including the one
# Supabase sends people back to after a magic link.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
